package day31_Constructors;

public class ConstructorIntro {

    /*
    - Without the constructor you can not create any object from classes
    - Every single class they MUST have constructor, because class is where the object came from
    - In java there is no class that does not have constructor
    - Constructor is a MUST
    - Constructor name has to be same with ClassName

    - Syntax
        Access-Modifier ClassName(){

            }
    - it used for creating objects
    - Execution depends on the object


     */

}
