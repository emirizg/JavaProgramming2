package day33_Statics;

public class Employee {

    /*
    - if one copy of element will be enough?
    - do I need one copy or more?
    - instance means --> there can be multiple copies
            * Each object has own memory
    - static means --> there can be only one copy, same value for all the objects, same for every single object
            * Each object has only one memory
            * Each object will be the same value
            * you just one time declare it and initialize it that's all

     */

    public String name;
    public double salary;

    public static boolean isHuman = true;






}
